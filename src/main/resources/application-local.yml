spring:
  config:
    activate:
      on-profile: local
  security:
    user:
      name: user
      password: localpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5434/kanionland_characters
    username: kanionland
    password: kanionland123
    driver-class-name: org.postgresql.Driver

server:
  port: 8000

logging:
  level:
    org.springframework: DEBUG
    com.kanionland: DEBUG

jwt:
  secret: ${JWT_SECRET}